<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ABSReader" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.ABSReader" parent="Base.Theme.ABSReader" />
</resources>